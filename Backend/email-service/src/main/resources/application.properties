spring.application.name=api

# MongoDB — reads from environment variable, falls back to local value
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://parshwajust55_db_user:Pa30%402005@cluster0.3wzwtqy.mongodb.net/invoice_db?appName=Cluster0}
spring.mongodb.uri=${MONGODB_URI:mongodb+srv://parshwajust55_db_user:Pa30%402005@cluster0.3wzwtqy.mongodb.net/invoice_db?appName=Cluster0}

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:invoicegenservice@gmail.com}
spring.mail.password=${MAIL_PASSWORD:awquaddhlsxthieb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Clerk JWT
spring.security.oauth2.resourceserver.jwt.issuer-uri=${CLERK_ISSUER_URI:https://expert-mudfish-7241.clerk.accounts.dev}

# Server port (Render assigns a PORT env variable)
server.port=${PORT:8080}

